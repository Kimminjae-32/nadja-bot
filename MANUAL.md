# 나쟈봇 사용 매뉴얼

이터널 리턴 내전 운영을 도와주는 디스코드 봇입니다.  
참가 신청부터 팀 배정, 음성 채널 이동까지 전 과정을 웹 페이지에서 관리합니다.

---

## 목차

1. [봇 명령어](#1-봇-명령어)
2. [내전 만들기](#2-내전-만들기)
3. [참가자 가이드](#3-참가자-가이드)
4. [방장 관리 페이지](#4-방장-관리-페이지)
   - [팀 배정](#41-팀-배정)
   - [팀경매 (드래프트)](#42-팀경매-드래프트)
   - [캐릭터 밴 & 실험체 배정](#43-캐릭터-밴--실험체-배정)
   - [음성 채널 이동](#44-음성-채널-이동)
   - [맵 변경](#45-맵-변경)
   - [방장 양도](#46-방장-양도)
   - [내전 종료](#47-내전-종료)
5. [정보 명령어](#5-정보-명령어)
6. [전체 흐름도](#6-전체-흐름도)

---

## 1. 봇 명령어

| 명령어 | 설명 |
|---|---|
| `/내전 루미아 섬` | 루미아 섬 내전 생성 (팀 수·인원 자유 설정) |
| `/내전 코발트` | 코발트 내전 생성 (2팀 4vs4 고정) |
| `/내전 론울프` | 론울프 개인전 생성 (최대 18명) |
| `/구인` | 일반/랭크 파티원 구인글 생성 |
| `/사용법` | 봇 기능 요약 안내 |
| `/시즌` | 현재 이터널 리턴 시즌 정보 및 종료일 |
| `/무료실험체` | 이번 주 무료 실험체 목록 |

---

## 2. 내전 만들기

### 모드 선택

#### 루미아 섬 내전
```
/내전 루미아 섬  팀당인원: 3  최대인원: 24  시간: 21:00  종료시간: 6
```
- **팀당인원**: 한 팀에 몇 명인지 (예: 3이면 3vs3vs3...)
- **최대인원**: 총 몇 명까지 받을지
- 팀 수는 `최대인원 ÷ 팀당인원`으로 자동 계산 (최대 8팀)
- 최대인원이 팀당인원으로 딱 나누어지지 않으면 선택지가 표시됨

#### 코발트 내전
```
/내전 코발트  시간: 21:00
```
- 2팀, 팀당 4명, 총 8명으로 자동 고정

#### 론울프 (개인전)
```
/내전 론울프  최대인원: 12  시간: 21:00
```
- 1인 1팀 개인전 방식 (최대 18명)
- 포지션 선택 없음

### 공통 옵션

| 옵션 | 설명 | 기본값 |
|---|---|---|
| `시간` | 시작 시간 표시 (예: `21:00`, `즉시`) | 즉시 |
| `종료시간` | 몇 시간 후 자동 삭제 | 24시간 |

### 생성 후 화면

명령어 실행 시 채널에 **구인 임베드**가 올라옵니다.

```
🎮 [내전 / 루미아 섬] 구인 중
⏰ 시작 시간: 21:00  👥 인원: 1/24  👑 모집자: OOO
📝 전체 참가자: OOO

[ 참가/취소 ]
```

---

## 3. 참가자 가이드

### 참가 신청 흐름

```
디스코드에서 [참가/취소] 버튼 클릭
        ↓
웹 링크 안내 메시지 수신 (본인에게만 보임)
        ↓
링크 클릭 → 참가 신청 웹 폼 이동
        ↓
정보 입력 후 제출
        ↓
취소/수정 링크 화면 표시
```

### 웹 폼 입력 항목

| 항목 | 필수 여부 | 설명 |
|---|---|---|
| 디스코드 닉네임 | 필수 | 표시될 이름 |
| 인게임 닉네임 | 필수 | 이터널 리턴 인게임 ID |
| 선호 포지션 | 선택 (론울프 제외) | 탱커/전사/암살자/스킬 딜러/원거리 딜러/지원가 |
| 티어 | 선택 | 아이언~이터니티 |

### 이미 신청한 경우

`[참가/취소]` 버튼을 다시 누르면 **수정·취소 페이지 링크**가 안내됩니다.  
수정 페이지에서 정보 변경 또는 참가 취소를 할 수 있습니다.

> **팁**: 방장인 경우 수정 페이지 상단에 **[관리자 페이지 열기]** 버튼이 표시됩니다.

---

## 4. 방장 관리 페이지

### 접속 방법

1. 내전을 생성한 사람이 `[참가/취소]` 버튼 클릭
2. 수정/취소 링크 열기
3. 상단의 **"⚙️ 내전 관리자 페이지 열기"** 클릭

> 방장 양도 시 새 방장의 디스코드로 관리 페이지 링크가 DM으로 자동 전송됩니다.

---

### 4.1 팀 배정

참가자를 팀에 배정하는 방법은 두 가지입니다.

#### 자동 셔플

```
[🎲 팀 자동 배정] 클릭
→ 전체 참가자를 무작위로 팀에 배분
```

#### 수동 배정

- 각 참가자 행에서 팀 번호 선택 또는 드래그
- 특정 참가자를 원하는 팀에 직접 지정

#### 팀 배정 결과 전송

```
[📢 Discord에 결과 전송] 클릭
→ 채널에 팀별 참가자 목록 임베드 전송
```

---

### 4.2 팀경매 (드래프트)

팀장이 순서대로 팀원을 한 명씩 직접 선택하는 방식입니다.

> **주의**: 방장도 드래프트에 참여하려면 반드시 `[참가/취소]` 버튼을 눌러 **웹 폼을 직접 제출**해야 선택지에 나타납니다.

#### 흐름

```
1. 팀장 지정
   → 각 팀의 팀장 역할을 할 참가자 선택

2. 드래프트 시작
   → 팀장들에게 픽 링크가 DM으로 자동 전송

3. 팀장이 픽 링크 접속
   → 자기 차례에 남은 참가자 중 1명 선택

4. 모든 참가자가 팀에 배정될 때까지 반복
   (1팀 → 2팀 → 3팀 → ... → 순환)
```

> 픽 순서는 팀 번호 순으로 순환합니다. (예: 1팀 → 2팀 → 1팀 → 2팀 ...)

---

### 4.3 캐릭터 밴 & 실험체 배정

#### 캐릭터 밴

```
[+ 캐릭터 밴 추가] 클릭
→ 캐릭터 검색 또는 목록에서 선택
→ 선택한 캐릭터가 밴 목록에 추가됨
→ 칩의 [✕] 클릭으로 밴 취소
```

밴된 캐릭터는 실험체 랜덤 배정 시 제외됩니다.

#### 실험체 랜덤 배정

```
[🎲 실험체 랜덤 배정] 클릭
→ 팀 배정된 참가자에게 실험체 무작위 배정
→ 밴된 캐릭터 제외하고 배정
→ 결과 목록 화면에 표시
```

---

### 4.4 음성 채널 이동

참가자들을 팀별 음성 채널로 한 번에 이동시킵니다.

#### 사전 조건

- 참가자들이 팀에 배정되어 있어야 함
- 참가자들이 **현재 디스코드 음성 채널에 접속 중**이어야 함
- 봇이 해당 서버에서 **멤버 이동** 권한을 가지고 있어야 함

#### 흐름

```
각 팀에 이동할 음성 채널 선택
        ↓
[🚀 이동 실행] 클릭
        ↓
각 팀원이 지정된 채널로 자동 이동
        ↓
경기 종료 후 [↩️ 원래 채널로] 클릭
        ↓
이동 전 채널로 전원 복귀
```

> **주의**: 이미 채널에 없는 참가자(음성 미접속)는 이동되지 않습니다.

---

### 4.5 맵 변경

진행 중인 내전의 맵/모드를 바꿀 수 있습니다. 디스코드 메시지도 자동으로 업데이트됩니다.

| 변경 대상 | 설정값 |
|---|---|
| 루미아 섬 | 팀당 인원·최대 인원 재설정 |
| 코발트 | 2팀 4vs4 고정으로 변경 |
| 론울프 | 최대 인원만 설정 (1인 1팀) |

> 맵 변경 시 기존 팀 배정은 초기화됩니다.

---

### 4.6 방장 양도

다른 참가자에게 방장 권한을 넘깁니다.

```
양도할 참가자 선택 (드롭다운)
        ↓
[양도] 클릭
        ↓
선택된 참가자의 디스코드로 관리 페이지 링크 DM 자동 전송
```

> DM 수신을 차단한 경우 메시지가 전달되지 않을 수 있습니다.

---

### 4.7 내전 종료

```
[🗑️ 내전 종료] 클릭 → 확인
        ↓
디스코드 구인 메시지 삭제
모든 참가 데이터 삭제
관리 페이지 접근 불가
```

> 생성 시 설정한 시간(기본 24시간)이 지나면 자동으로 종료됩니다.

---

## 5. 정보 명령어

### `/시즌`

현재 이터널 리턴 시즌 정보를 표시합니다.

```
시즌: Season 24
시작일: 2026-03-15
종료일: 2026-07-01
남은 기간: ⏳ 14일 남음
```

### `/무료실험체`

이번 주 모드별 무료 실험체 목록을 표시합니다.

```
일반: 재키, 아야, 현우, ...
코발트: 피오라, 나딘, ...
```

### `/구인`

파티원을 구하는 구인글을 생성합니다.

| 유형 | 인원 |
|---|---|
| 일반 (루미아) | 3명 |
| 랭크 (루미아) | 3명 |
| 일반 (코발트) | 4명 |

---

## 6. 전체 흐름도

### 일반 내전 흐름

```
방장: /내전 루미아 섬 입력
            │
            ▼
    디스코드 구인 임베드 생성
    [참가/취소 버튼] 표시
            │
     ┌──────┴──────┐
     │             │
  방장 참가      참가자 버튼 클릭
     │             │
     │         웹 폼 작성
     │         닉네임/티어/포지션
     │             │
     └──────┬──────┘
            │
    참가자 모집 완료
            │
            ▼
    방장 → 관리자 페이지 접속
            │
     ┌──────┴──────────────┐
     │                     │
  자동 셔플             팀경매 시작
  (랜덤 팀 배정)        팀장 선택 → DM 전송
     │                     │
     │                팀장들이 순서대로 픽
     └──────┬──────────────┘
            │
    (선택) 캐릭터 밴 설정
            │
    (선택) 실험체 랜덤 배정
            │
            ▼
    음성 채널 이동 실행
    팀별 채널로 전원 이동
            │
            ▼
         경기 진행
            │
            ▼
    원래 채널로 복귀
            │
            ▼
    Discord에 결과 전송
            │
            ▼
        내전 종료
```

### 참가자 입장 흐름

```
디스코드에서 [참가/취소] 클릭
            │
    ┌───────┴────────┐
    │                │
처음 참가          이미 신청
    │                │
웹 폼 입력       취소 링크 안내
    │                │
   제출          수정 또는 취소
    │
취소/수정 페이지
    │
    ├── 방장인 경우: 관리자 페이지 버튼 표시
    │
    ├── 정보 수정하기
    └── 참가 취소하기
```

### 팀경매 흐름

```
방장이 팀경매 시작 (팀장 지정)
            │
            ▼
    팀장들에게 픽 링크 DM 전송
            │
            ▼
    1팀 팀장의 차례
    → 남은 참가자 중 1명 픽
            │
            ▼
    2팀 팀장의 차례
    → 남은 참가자 중 1명 픽
            │
           ...
            │
            ▼
    모든 참가자 배정 완료
```

---

## 자주 묻는 질문

**Q. 참가 신청 후 정보를 바꾸고 싶어요.**  
A. `[참가/취소]` 버튼을 다시 클릭하면 취소 링크가 나옵니다. 거기서 "정보 수정하기"를 클릭하세요.

**Q. 방장 관리 페이지 링크를 잃어버렸어요.**  
A. `[참가/취소]` 버튼을 클릭하면 수정 페이지로 이동하고, 거기서 "관리자 페이지 열기" 버튼이 표시됩니다.

**Q. 음성 채널 이동이 안 돼요.**  
A. 다음을 확인하세요.
- 참가자들이 디스코드 음성 채널에 접속 중인지
- 팀 배정이 되어 있는지
- 봇이 "멤버 이동" 권한을 가지고 있는지

**Q. 내전이 자동으로 사라졌어요.**  
A. 생성 시 설정한 종료 시간(기본 24시간)이 지나면 자동 삭제됩니다. 생성 시 `종료시간` 옵션으로 조정할 수 있습니다.

**Q. 드래프트 팀장 선택지에 방장이 안 보여요.**  
A. 방장도 `[참가/취소]` 버튼을 눌러 웹 폼을 직접 제출해야 선택지에 나타납니다. 내전을 생성했다고 자동으로 등록되지 않습니다.

**Q. 방장을 양도했는데 새 방장이 링크를 못 받았어요.**  
A. 상대방의 디스코드 DM 설정을 확인해보세요. DM을 막아둔 경우 전송이 안 됩니다. 이 경우 취소 링크 페이지에서 수동으로 관리자 페이지에 접속할 수 있습니다.
